{
  "name": "sitecoleta-react",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://coletareact.vercel.app",  // Atualizado para URL real
  "dependencies": {
    // ... (mantenha todas as dependências existentes) ...
    "cors": "^2.8.5",                          // Adicione se não tiver
    "express": "^4.18.2",                       // Adicione para SSR (opcional)
    "path-browserify": "^1.0.1"                 // Adicione para resolver problemas de path
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "GENERATE_SOURCEMAP=false react-scripts build",  // Desativa sourcemaps
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "serve": "node server.js"                   // Novo script para produção
  },
  // ... (mantenha o restante igual) ...
}
